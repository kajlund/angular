(function () {
    'use strict';

    /**
     * Create a new module using the module setter method.
     * Second argument is mandatory and can contain modules we depend on
     */
    angular
        .module('app', []);
})();
